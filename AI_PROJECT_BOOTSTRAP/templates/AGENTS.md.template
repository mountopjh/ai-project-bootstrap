# 项目协作规范

## 执行授权

1. 收到任何具体任务后，先完整复述其目标、范围、约束和预期结果，并列出清晰、可核对的执行方案。
2. 等待人类确认理解是否准确、是否需要修改；不得代替人类或默认确认。
3. 仅在人类明确发送完整的“执行任务”四个字后才可执行；“开始吧”“可以”“继续”“没问题”等其他表达均不构成授权。
4. 获得授权前，只能复述需求、说明方案并询问是否确认；不得读取或改动项目文件、编写代码、运行命令或调用会读取项目、改变项目、产出任务成果的工具。

## 项目连续性

1. `AGENTS.md` 是唯一规则源；`PROJECT_INDEX.md` 是上下文入口；`DEVELOPMENT_MAP.md` 和 `CODE_MAP.md` 分别保存当前开发状态与代码结构。
2. 获得执行授权后先读 `PROJECT_INDEX.md`，再按其路由读取当前任务所需的最少文件；历史归档仅在追溯时读取。
3. 实际开发结束前，仅在内容变化时更新相关地图。阶段结束、架构变化或覆盖重要历史前，先以 `YYYYMMDD-HHMMSS` 命名归档快照并更新索引；归档只读。
4. 规则变更时删除失效、冲突、重复或无实际作用的内容；地图和索引不得重复维护规则，且必须足以让新对话识别当前进展和下一项任务。

## 时间格式

项目中所有涉及时间的内容都必须精确到秒，并统一使用 `YYYYMMDD-HHMMSS`；文档、地图、状态、任务、变更、日志、时间字段及含时间的文件名或标识均不得使用其他格式。
