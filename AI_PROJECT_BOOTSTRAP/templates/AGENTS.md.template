# 项目协作规范

## 指令与入口

1. 遵循当前 AI 平台的系统、开发者、工具安全要求以及用户明确指令；发生冲突时按更高优先级执行并说明，项目规则不得声称覆盖上级指令。
2. `AGENTS.md` 是唯一项目级规则源；`START_HERE.md` 只提供入口，`PROJECT_INDEX.md` 只负责上下文路由，地图和索引只记录项目状态，不得复制本文件规则。
3. 除非任务明确要求维护启动器本身，否则不得扫描或分析 `AI_PROJECT_BOOTSTRAP/` 源码；它不是业务项目上下文。

## 启动器初始化

1. 当用户的整条消息去除首尾空白和常见句末标点后，完整等于“初始化”或“初始化启动器”时，必须立即在当前项目根目录运行启动器，不得只解释命令或再要求用户发送“执行任务”。
2. 上述两个精确短语只授权本地启动器的初始化、无损修复和完整性检查，是下方“执行授权”的唯一例外；不授权修改业务代码、安装依赖、提交推送或其他扩展操作。
3. Windows 上优先运行 `pwsh -NoProfile -File .\start.ps1`；若 `pwsh` 不可用但 Python 3.8+ 可用，则运行 `python start.py`。macOS 或 Linux 上优先运行 `python3 start.py`，再回退到 `python start.py`。只做必要的可用性检测，不扫描启动器源码。
4. 启动命令成功后，报告初始化或修复结果，并明确提示 Codex 项目本地 `.codex/hooks.json` 钩子仍须由用户完成一次安全审核与信任，随后新建会话才会开始自动归档；在信任完成前不得声称自动保存已生效。
5. 若 IDE 无法执行本地命令，或本机缺少 PowerShell 7 与 Python 3.8+，只报告确切阻塞原因和可由用户手动执行的命令，不得假称初始化成功。

## 执行授权

1. 纯咨询、解释、状态询问和不产生项目或外部状态变更的只读审查，可直接读取最少必要文件并回答；用户明确要求“只复述”时不得执行检查或工具调用。凡诉求涉及项目开发、代码开发、代码修改或增加功能的，均不属于纯咨询，严禁跳过复述直接给出实现或执行。
2. 凡涉及项目开发、代码开发、代码修改（含重构优化与缺陷修复）、增加功能（含新增特性与模块），以及任何会写入文件、运行变更性命令、安装依赖、提交推送或改变外部状态的任务，均必须在给出方案或执行前，先复述目标、范围、约束、预期结果和可核对方案，再等待人类确认。
3. 仅在人类明确发送完整的“执行任务”四个字后才可执行上述变更与开发任务；“开始吧”“可以”“继续”“没问题”等其他表达均不构成授权。
4. 授权仅覆盖已复述范围，并持续到任务完成、取消、暂停或范围发生实质变化；范围内的后续步骤无需重复授权，新增或扩大范围必须重新复述并获得授权。
5. 变更与开发任务授权前，只能读取 `START_HERE.md`、`AGENTS.md` 和用户直接提供的内容；不得读取业务文件、运行命令或产出实施成果。

## 执行边界与安全

1. 默认从当前项目根目录启动命令；必要时可在项目子目录或隔离的系统临时目录操作并及时清理。除非用户明确授权，不得操作项目外或无关目录。
2. 修改必须限于任务范围，保留用户已有改动，不覆盖或清理无关文件；工作区存在冲突时先说明。
3. 不得泄露、写入或提交密钥、令牌、隐私数据及其他敏感信息；本地对话归档不得提交到版本库。
4. 执行删除、覆盖、强制更新或其他难以恢复的操作前，必须确认精确目标和影响；若已复述并授权的范围明确包含该操作，不重复索取授权。
5. 修改后运行与风险相称的测试和完整性检查；无法验证时说明原因。交付时报告结果、验证情况、未验证项和外部状态变化。

## 项目连续性

1. 获得变更授权后先读 `PROJECT_INDEX.md`，再按路由读取当前任务所需的最少文件；历史归档仅在追溯时读取。
2. 仅在项目状态、关键结构或下一步发生实质变化时更新 `DEVELOPMENT_MAP.md`、`CODE_MAP.md` 和相关索引，避免为无影响的小改动制造维护噪声。
3. 阶段结束、架构变化或覆盖重要历史前，以 `YYYYMMDD-HHMMSS_简介` 创建归档快照并更新索引；简介不超过10字且能识别内容。
4. 已生成的归档正文不可修改；对应索引可以追加条目和更新时间。地图与索引必须足以让新对话识别当前进展和下一项任务。

## 命名与放置

1. 人工新增的持久文件应使用可识别其功能的名称，禁止 `temp`、`test1`、`new`、`未命名`、`副本`、`aaa`、`123` 等无信息量命名。
2. 生态、框架或工具规定的标准文件名、入口名、迁移编号和自动生成文件可以遵循其既有约定。
3. 文件按功能职责放置；标准根目录配置文件可以保留在根目录，临时文件应放入隔离临时目录或专用临时目录并在任务结束时清理。
4. 仅当关键文件或已被 `CODE_MAP.md` 跟踪的文件发生命名、位置或职责变化时同步更新代码地图。

## 时间格式

项目治理文档、地图、归档、启动器状态和相关文件名统一使用精确到秒的 `YYYYMMDD-HHMMSS`。业务数据、接口协议、数据库、第三方工具和生态标准要求其他格式时遵循其规范，不强制转换。
