# AI 项目入口

项目名称：`{{PROJECT_NAME}}`

无论使用哪一种AI，开始项目前都必须读取项目根目录的 `AGENTS.md`，并把它视为唯一规则源。

随后按 `PROJECT_INDEX.md` 的路由加载当前任务所需的最少上下文。`AI_PROJECT_BOOTSTRAP/` 是启动器实现目录，仅在维护启动器时进入，不属于默认业务上下文。

## IDE 对话归档

- Codex：本项目已生成 `.codex/hooks.json`。在信任该本地钩子并开启新会话后，每轮“用户消息 + AI 回答”会自动保存到 `archive/conversations/`，不调用模型或外部 API。
- Kiro：本项目已生成 `.kiro/hooks/archive-conversation.kiro.hook` 并默认启用，每轮对话结束（`agentStop`）会自动调用本地记录工具归档，无需额外信任步骤。
- 其他 AI IDE：从本文件进入并读取 `AGENTS.md`；如 IDE 没有可用的生命周期钩子，可在每轮结束后调用项目内的 `tools/record-conversation.ps1` 或 `tools/record_conversation.py` 保存对话。
