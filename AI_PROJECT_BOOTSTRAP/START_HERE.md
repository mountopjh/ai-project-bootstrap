# 初始化器入口

这是“通用 AI 项目启动器”的入口文件。

空项目目录直接克隆完整仓库后，请回到仓库根目录运行 `pwsh -NoProfile -File .\start.ps1` 或 `python start.py`。初始化目标项目后，任何AI都应从目标项目的 `START_HERE.md` 开始，并以目标项目的 `AGENTS.md` 为唯一规则源。

不要逐个分析本目录的源码，也不要把本目录直接当作业务项目规则；一键入口会将模板安装到目标项目并执行完整性检查。只有维护启动器本身时才需要读取这里的实现文件。
