# 启动器首次初始化

<!-- AI_PROJECT_BOOTSTRAP_PREINIT -->

本文件只负责让支持 `AGENTS.md` 的 AI IDE 在仓库首次下载、尚未生成完整项目规则前启动本地初始化器。

当用户的整条消息去除首尾空白和常见句末标点后，完整等于“初始化”或“初始化启动器”时：

1. 这两个精确短语即为运行本地启动器的明确授权；不要只解释命令，也不要再次询问是否执行。
2. 必须从当前项目根目录运行启动器。Windows 优先执行 `pwsh -NoProfile -File .\start.ps1`，缺少 PowerShell 7 时执行 `python start.py`；macOS 或 Linux 优先执行 `python3 start.py`，再回退到 `python start.py`。
3. 不扫描或分析 `AI_PROJECT_BOOTSTRAP/` 源码。启动脚本会自动完成首次初始化或无损修复，并执行完整性检查。
4. 成功后说明规则、索引、归档工具和 `.codex/hooks.json` 已生成。Codex 的项目本地钩子仍须由用户完成一次安全审核与信任，并在新会话中生效；不得绕过或假称已经完成信任。
5. 除启动器初始化、修复和检查外，这两个短语不授权修改业务代码、安装依赖、提交推送或改变其他外部状态。

其他请求只作正常回答；首次初始化会用完整的项目协作规范替换本文件。
